{
  "mcpServers": {
    "gis": {
      "command": "uvx",
      "args": ["gis-mcp"]
    },
    "qgis": {
      "command": "uvx",
      "args": ["qgis-mcp"]
    },
    "nasa": {
      "command": "npx",
      "args": ["-y", "@programcomputer/nasa-mcp-server@latest"],
      "env": {
        "NASA_API_KEY": "${NASA_TOKEN}"
      }
    },
    "mapbox": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-mapbox"],
      "env": {
        "MAPBOX_ACCESS_TOKEN": "${MAPBOX_API_KEY}"
      }
    }
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo [ATLAS] GIS Command Center initialized - 12 skills, 7 agents, 4 MCP servers standing by"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If this Write created a spatial analysis report, GIS output, map product, or remote sensing analysis, invoke the gis-fact-checker skill to verify all coordinates, projections, area calculations, data sources, and classification accuracy before considering the task complete."
          }
        ]
      }
    ]
  }
}
